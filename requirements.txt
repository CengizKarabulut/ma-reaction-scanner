# Veri kaynakları
borsapy>=0.10.0    # BIST + sektör + endeks bileşenleri + intraday (TradingView WS)
yfinance>=0.2.0    # Yedek + sektör fallback

# Veri işleme
pandas>=2.0
numpy

# HTTP scraping (bist_data_fetcher - ISYatırım/mynet yedek)
httpx>=0.27.0

# Plot ve image üretimi (notifier image-rich mod için)
matplotlib

# PDF, rapor
reportlab
