# Veri kaynakları
borsapy>=0.10.0,<1    # BIST + sektör + endeks bileşenleri + intraday (TradingView WS)
yfinance>=0.2.0,<1    # Yedek + sektör fallback

# Veri işleme
pandas>=2.0,<3
numpy>=1.26,<3

# HTTP scraping (bist_data_fetcher - ISYatırım/mynet yedek)
httpx>=0.27.0,<1

# Plot ve image üretimi (notifier image-rich mod için)
matplotlib>=3.8,<4

# PDF, rapor
reportlab>=4,<5
tabulate>=0.9,<1
